import { ShowDescriptionDirective } from './show-description.directive';

describe('ShowDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
